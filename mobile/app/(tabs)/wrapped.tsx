import { View, Text } from "react-native";

export default function Wrapped() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Wrapped Screen</Text>
    </View>
  );
}